UC2 — Print a Hardcoded Palindrome Result
Overview
Check a hardcoded word (e.g., "madam") and print whether it is a palindrome.
Learning Goals

Use of String and string literals.
Simple conditional logic (if-else).
Equality check with equals.
